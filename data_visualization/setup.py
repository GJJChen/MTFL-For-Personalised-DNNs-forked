# data_visualization/setup.py

from setuptools import setup, find_packages

setup(
    name='data_visualization',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'matplotlib',
        'numpy',
        # any other dependencies required
    ],
)
